import 'package:flutter/material.dart';

class GraftResouces extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(20.0),
      child: SizedBox(
        width: 450,
        height: 300,
        child: Container(
          color: Colors.black,
        ),
      ),
    );
  }
}
