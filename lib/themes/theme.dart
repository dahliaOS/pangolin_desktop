import 'package:flutter/material.dart';

class Themes {
  //dark
  ThemeData darkOrange = ThemeData(
      brightness: Brightness.dark,
      sliderTheme: SliderThemeData(
        activeTickMarkColor: Colors.deepOrangeAccent[400],
        thumbColor: Colors.deepOrangeAccent[400],
        activeTrackColor: Colors.deepOrangeAccent[400],
        inactiveTrackColor: Colors.deepOrangeAccent[400].withAlpha(90),
        inactiveTickMarkColor: Colors.deepOrangeAccent[400],
      ),
      accentColor: Colors.deepOrangeAccent[400],
      primaryColor: Colors.deepOrangeAccent[400],
      primarySwatch: Colors.deepOrangeAccent[400],
      platform: TargetPlatform.android);

  ThemeData darkRed = new ThemeData(
      brightness: Brightness.dark,
      sliderTheme: SliderThemeData(
        activeTickMarkColor: Colors.redAccent[700],
        thumbColor: Colors.redAccent[700],
        activeTrackColor: Colors.redAccent[700],
        inactiveTrackColor: Colors.redAccent[700].withAlpha(90),
        inactiveTickMarkColor: Colors.redAccent[700],
      ),
      accentColor: Colors.redAccent[700],
      primaryColor: Colors.redAccent[700],
      primarySwatch: Colors.redAccent[700],
      platform: TargetPlatform.android);

  ThemeData darkGreen = new ThemeData(
      brightness: Brightness.dark,
      sliderTheme: SliderThemeData(
        activeTickMarkColor: Colors.greenAccent[700],
        thumbColor: Colors.greenAccent[700],
        activeTrackColor: Colors.greenAccent[700],
        inactiveTrackColor: Colors.greenAccent[700].withAlpha(90),
        inactiveTickMarkColor: Colors.greenAccent[700],
      ),
      accentColor: Colors.greenAccent[700],
      primaryColor: Colors.greenAccent[700],
      primarySwatch: Colors.greenAccent[700],
      platform: TargetPlatform.android);

  ThemeData darkBlue = new ThemeData(
      brightness: Brightness.dark,
      sliderTheme: SliderThemeData(
        activeTickMarkColor: Colors.blueAccent[700],
        thumbColor: Colors.blueAccent[700],
        activeTrackColor: Colors.blueAccent[700],
        inactiveTrackColor: Colors.blueAccent[700].withAlpha(90),
        inactiveTickMarkColor: Colors.blueAccent[700],
      ),
      accentColor: Colors.blueAccent[700],
      primaryColor: Colors.blueAccent[700],
      primarySwatch: Colors.blueAccent[700],
      platform: TargetPlatform.android);

  ThemeData darkPurple = new ThemeData(
      brightness: Brightness.dark,
      sliderTheme: SliderThemeData(
        activeTickMarkColor: Colors.purpleAccent[700],
        thumbColor: Colors.purpleAccent[700],
        activeTrackColor: Colors.purpleAccent[700],
        inactiveTrackColor: Colors.purpleAccent[700].withAlpha(90),
        inactiveTickMarkColor: Colors.purpleAccent[700],
      ),
      accentColor: Colors.purpleAccent[700],
      primaryColor: Colors.purpleAccent[700],
      primarySwatch: Colors.purpleAccent[700],
      platform: TargetPlatform.android);

  ThemeData darkCyan = new ThemeData(
      brightness: Brightness.dark,
      sliderTheme: SliderThemeData(
        activeTickMarkColor: Colors.cyanAccent[700],
        thumbColor: Colors.cyanAccent[700],
        activeTrackColor: Colors.cyanAccent[700],
        inactiveTrackColor: Colors.cyanAccent[700].withAlpha(90),
        inactiveTickMarkColor: Colors.cyanAccent[700],
      ),
      accentColor: Colors.cyanAccent[700],
      primaryColor: Colors.cyanAccent[700],
      primarySwatch: Colors.cyanAccent[700],
      platform: TargetPlatform.android);

  ThemeData darkAmber = new ThemeData(
      brightness: Brightness.dark,
      sliderTheme: SliderThemeData(
        activeTickMarkColor: Colors.amberAccent[700],
        thumbColor: Colors.amberAccent[700],
        activeTrackColor: Colors.amberAccent[700],
        inactiveTrackColor: Colors.amberAccent[700].withAlpha(90),
        inactiveTickMarkColor: Colors.amberAccent[700],
      ),
      accentColor: Colors.amberAccent[700],
      primaryColor: Colors.amberAccent[700],
      primarySwatch: Colors.amberAccent[700],
      platform: TargetPlatform.android);

  ThemeData darkBlack = new ThemeData(
      brightness: Brightness.dark,
      sliderTheme: SliderThemeData(
          activeTickMarkColor: Colors.black,
          thumbColor: Colors.black,
          activeTrackColor: Colors.black,
          inactiveTrackColor: Colors.black.withAlpha(90),
          inactiveTickMarkColor: Colors.black),
      accentColor: Colors.black,
      primaryColor: Colors.black,
      primarySwatch: Colors.black,
      platform: TargetPlatform.android);

  //light
  ThemeData lightOrange = new ThemeData(
      brightness: Brightness.light,
      sliderTheme: SliderThemeData(
        activeTickMarkColor: Colors.deepOrangeAccent[400],
        thumbColor: Colors.deepOrangeAccent[400],
        activeTrackColor: Colors.deepOrangeAccent[400],
        inactiveTrackColor: Colors.deepOrangeAccent[400].withAlpha(90),
        inactiveTickMarkColor: Colors.deepOrangeAccent[400],
      ),
      accentColor: Colors.deepOrangeAccent[400],
      primaryColor: Colors.deepOrangeAccent[400],
      //primarySwatch: Colors.deepOrangeAccent[400],
      platform: TargetPlatform.android);

  ThemeData lightRed = new ThemeData(
      brightness: Brightness.light,
      sliderTheme: SliderThemeData(
        activeTickMarkColor: Colors.redAccent[700],
        thumbColor: Colors.redAccent[700],
        activeTrackColor: Colors.redAccent[700],
        inactiveTrackColor: Colors.redAccent[700].withAlpha(90),
        inactiveTickMarkColor: Colors.redAccent[700],
      ),
      accentColor: Colors.redAccent[700],
      primaryColor: Colors.redAccent[700],
      primarySwatch: Colors.redAccent[700],
      platform: TargetPlatform.android);

  ThemeData lightGreen = new ThemeData(
      brightness: Brightness.light,
      sliderTheme: SliderThemeData(
        activeTickMarkColor: Colors.greenAccent[700],
        thumbColor: Colors.greenAccent[700],
        activeTrackColor: Colors.greenAccent[700],
        inactiveTrackColor: Colors.greenAccent[700].withAlpha(90),
        inactiveTickMarkColor: Colors.greenAccent[700],
      ),
      accentColor: Colors.greenAccent[700],
      primaryColor: Colors.greenAccent[700],
      primarySwatch: Colors.greenAccent[700],
      platform: TargetPlatform.android);

  ThemeData lightBlue = new ThemeData(
      brightness: Brightness.light,
      sliderTheme: SliderThemeData(
        activeTickMarkColor: Colors.blueAccent[700],
        thumbColor: Colors.blueAccent[700],
        activeTrackColor: Colors.blueAccent[700],
        inactiveTrackColor: Colors.blueAccent[700].withAlpha(90),
        inactiveTickMarkColor: Colors.blueAccent[700],
      ),
      accentColor: Colors.blueAccent[700],
      primaryColor: Colors.blueAccent[700],
      primarySwatch: Colors.blueAccent[700],
      platform: TargetPlatform.android);

  ThemeData lightPurple = new ThemeData(
      brightness: Brightness.light,
      sliderTheme: SliderThemeData(
        activeTickMarkColor: Colors.purpleAccent[700],
        thumbColor: Colors.purpleAccent[700],
        activeTrackColor: Colors.purpleAccent[700],
        inactiveTrackColor: Colors.purpleAccent[700].withAlpha(90),
        inactiveTickMarkColor: Colors.purpleAccent[700],
      ),
      accentColor: Colors.purpleAccent[700],
      primaryColor: Colors.purpleAccent[700],
      primarySwatch: Colors.purpleAccent[700],
      platform: TargetPlatform.android);

  ThemeData lightCyan = new ThemeData(
      brightness: Brightness.light,
      sliderTheme: SliderThemeData(
        activeTickMarkColor: Colors.cyanAccent[700],
        thumbColor: Colors.cyanAccent[700],
        activeTrackColor: Colors.cyanAccent[700],
        inactiveTrackColor: Colors.cyanAccent[700].withAlpha(90),
        inactiveTickMarkColor: Colors.cyanAccent[700],
      ),
      accentColor: Colors.cyanAccent[700],
      primaryColor: Colors.cyanAccent[700],
      primarySwatch: Colors.cyanAccent[700],
      platform: TargetPlatform.android);

  ThemeData lightAmber = new ThemeData(
      brightness: Brightness.light,
      sliderTheme: SliderThemeData(
        activeTickMarkColor: Colors.amberAccent[700],
        thumbColor: Colors.amberAccent[700],
        activeTrackColor: Colors.amberAccent[700],
        inactiveTrackColor: Colors.amberAccent[700].withAlpha(90),
        inactiveTickMarkColor: Colors.amberAccent[700],
      ),
      accentColor: Colors.amberAccent[700],
      primaryColor: Colors.amberAccent[700],
      primarySwatch: Colors.amberAccent[700],
      platform: TargetPlatform.android);

  ThemeData lightBlack = new ThemeData(
      brightness: Brightness.light,
      sliderTheme: SliderThemeData(
          activeTickMarkColor: Colors.black,
          thumbColor: Colors.black,
          activeTrackColor: Colors.black,
          inactiveTrackColor: Colors.black.withAlpha(90),
          inactiveTickMarkColor: Colors.black),
      accentColor: Colors.black,
      primaryColor: Colors.black,
      primarySwatch: Colors.black,
      platform: TargetPlatform.android);
}
