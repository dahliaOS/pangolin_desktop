name: dahlia_backend
description: Backend for the pangolin desktop
version: 0.0.1+1
publish_to: 'none'
homepage: https://dahliaos.io

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  path_provider: any
  hive: ^2.0.0
  intl: ^0.17.0-nullsafety.2
  hive_flutter: any
  utopia_wm:
    git:
      url: https://github.com/dahliaOS/utopia.git
      ref: utopia

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
  vm_service: ^6.1.0+1
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - ../assets/textures